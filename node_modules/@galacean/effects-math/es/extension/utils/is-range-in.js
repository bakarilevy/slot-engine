export var IntervalType;
(function (IntervalType) {
    IntervalType[IntervalType["OPEN"] = 0] = "OPEN";
    IntervalType[IntervalType["CLOSE"] = 1] = "CLOSE";
    IntervalType[IntervalType["LEFT_CLOSE"] = 2] = "LEFT_CLOSE";
    IntervalType[IntervalType["RIGHT_CLOSE"] = 3] = "RIGHT_CLOSE";
})(IntervalType || (IntervalType = {}));
export function isRangeIn(num, min, max, intervalType = IntervalType.OPEN) {
    switch (intervalType) {
        case IntervalType.OPEN:
            return num > min && num < max;
        case IntervalType.CLOSE:
            return num >= min && num <= max;
        case IntervalType.LEFT_CLOSE:
            return num >= min && num < max;
        case IntervalType.RIGHT_CLOSE:
            return num > min && num <= max;
    }
}

export function intMultiplyFloat(int, floatMultiplier, precision = 1) {
    if (floatMultiplier % 1 !== 0) {
        return intMultiplyFloat(int, floatMultiplier * 10, precision * 10);
    }
    return (int * floatMultiplier) / precision;
}
